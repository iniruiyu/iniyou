# Legacy Web static site server / Legacy Web 静态站点服务器
server {
	listen 80 default_server;
	listen [::]:80 default_server;
	server_name _;

	root /var/lib/nginx/html;
	index index.html;

	location / {
		try_files $uri $uri/ /index.html;
	}
}
