JWT_SECRET=dev-secret
TOKEN_TTL_MIN=120
DB_DSN=host=localhost user=postgres password=postgres dbname=account_service port=5432 sslmode=disable

# SERVICE_PORT is optional.
# Account service defaults to 8080, message service defaults to 8081.
# SERVICE_PORT 为可选项。
# 账号服务默认使用 8080，通讯服务默认使用 8081。
# SERVICE_PORT=8080
